{
  "name": "fluid-vibe",
  "version": "0.0.1",
  "type": "module",
  "private": true,
  "description": "Browser semantic vibe->params matcher for Fluid (transformers.js + all-MiniLM-L6-v2). Lazy-loaded behind Fluid's Vibe input; runs 100% client-side, no backend.",
  "scripts": {
    "vectors": "node build-vectors.mjs",
    "test": "node test.mjs"
  },
  "devDependencies": {
    "@xenova/transformers": "^2.17.2"
  }
}

{
  "name": "fluid",
  "version": "0.1.0",
  "private": true,
  "description": "Generative art backgrounds \u2014 renders on the visitor's GPU",
  "scripts": {
    "dev": "npx wrangler dev --port 8787 --persist-to ../.fluid-dev-state",
    "deploy": "npx wrangler deploy",
    "test": "node --test tests/*.test.js"
  }
}
